COMPONENT InteractiveFormula:
- Use a valid, readable LaTeX formula in `latex` (or `formula`) and explain the meaning of each important variable in plain language.
- Provide `variables` with variable explanations and `derivationSteps` when they genuinely help the learner follow the mathematical reasoning.
